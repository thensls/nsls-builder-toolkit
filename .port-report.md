# Port report — prompt markdown rendering (track-studio → toolkit)

**Branch:** `feat/prompt-markdown-render` (off `origin/main`)
**Commit:** `d48e895` — fix(track-prototype): render prompt markdown in proto-player (sync from track-studio; mirror ignite-next AIPrompt)

## Regions ported

1. `renderMarkdown` + `inlineMd` (plus the safety/design comment block above them) — copied verbatim from
   `/Users/k/code/track-studio/lib/proto-player/lib/render-substep.mjs` into
   `skills/track-prototype/scripts/lib/render-substep.mjs`, placed in the same location (right after `safeUrl`,
   before the Icons section). Byte-diffed against the reference for this region — identical.
2. `promptBlock` — updated to the reference's version: main return now wraps `renderMarkdown(sub.prompt)` in
   `prose max-w-none prose-inherit` (data-tpl preserved), and the `banner-multiple` branch's main prompt div
   also uses `renderMarkdown(sub.prompt)` with `whitespace-pre-wrap` dropped. `bannerTexts` lines are untouched
   (still `esc()` + `whitespace-pre-wrap`).

No other block was touched — `titleBlock`, `calloutBlock`, `renderGenerate`'s prompt paragraph, the chat bubble,
celebration, etc. still use plain `esc()`, matching the reference's own scope.

A `diff` of the full files after the port came back empty — the toolkit's file is now byte-identical to
track-studio's reference for `render-substep.mjs`.

## Tests

Ported all substantive assertions from `render-substep.test.ts` into
`skills/track-prototype/scripts/lib/render-substep.test.mjs` (node:test, plain `.mjs`, double-quote/semicolon
style matching the existing file) — headings h1–h6, bold/italic/inline-code, `[t](url)` → target=_blank
rel="noopener noreferrer", unsafe-scheme link drop, lists, paragraph/soft-break handling, HTML-injection escaping,
`{token}` template-var passthrough, code-span verbatim (`` `**x**` ``, `` `foo_bar_baz.py` ``), link-URL-with-
markdown-chars intact, quote-in-URL no attribute breakout, nested `` [`code`](url) `` full restore with no leaked
PUA sentinel chars, authored-PUA-lookalike stripping, and `renderSubstep` integration (heading render + data-tpl,
prose-class wrapper, injection via full pipeline, template vars via full pipeline, banner-multiple markdown).

**Test command:** `node --test skills/track-prototype/scripts/lib/render-substep.test.mjs`
**Result:** all green — `# tests 59 / # pass 59 / # fail 0`

No `package.json` at the toolkit repo root (skills-only repo), so no separate `npm test` script exists; the
`node --test` invocation is the toolkit's test entry point for this file.

## Scope confirmation

`git diff --stat` shows exactly the two target files changed (174 / 173 lines added respectively, both purely
additive plus the `promptBlock` swap) — no other files touched, no design-kit rebuild run (not needed: markup
logic only, CSS `prose` already ships upstream).
