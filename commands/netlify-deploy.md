---
name: netlify-deploy
description: Deploy a static HTML file (or directory) to Netlify and visually review it with Playwright. Use after creating a fronten
---

Run the `netlify-deploy` skill. Use the Skill tool to invoke it: `Skill(netlify-deploy)`.
