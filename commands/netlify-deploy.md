---
name: netlify-deploy
description: Deploy a static HTML file or directory to Netlify and get a live preview URL in seconds.
---

Run the `netlify-deploy` skill. Use the Skill tool to invoke it: `Skill(netlify-deploy)`.
