---
name: nsls-focus-group
description: Run a simulated focus group to get feedback on NSLS people ops interactions. Uses persona-matched panels from customer segments.
---

Run the `nsls-focus-group` skill. Use the Skill tool to invoke it: `Skill(nsls-focus-group)`.
