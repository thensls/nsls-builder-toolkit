---
name: setup
description: >-
  Onboarding for the NSLS Builder Toolkit — checks plugin health, verifies
  tool connections, and optionally installs personal productivity skills.
---

Run the `setup` skill. Use the Skill tool to invoke it: `Skill(setup)`.
